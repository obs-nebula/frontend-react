version: "2"
services:
  front-end:
    build:
      context: ./front-end
      dockerfile: dockerFile
    ports:
      - "3000:3000"
    depends_on:
      - express-server
  express-server:
    build:
      context: ./express-server
      dockerfile: dockerFile
    ports:
      - "5000:5000"